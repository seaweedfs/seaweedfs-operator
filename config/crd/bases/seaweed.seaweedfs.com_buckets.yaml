---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: buckets.seaweed.seaweedfs.com
spec:
  group: seaweed.seaweedfs.com
  names:
    categories:
    - seaweedfs
    kind: Bucket
    listKind: BucketList
    plural: buckets
    shortNames:
    - swb
    singular: bucket
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.versioning
      name: Versioning
      type: string
    - jsonPath: .status.objectLockEnabled
      name: ObjectLock
      type: boolean
    - jsonPath: .status.quota.sizeBytes
      name: QuotaBytes
      type: integer
    - jsonPath: .status.usage.sizeBytes
      name: UsedBytes
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              access:
                items:
                  properties:
                    actions:
                      items:
                        enum:
                        - Read
                        - Write
                        - List
                        - Tagging
                        - Admin
                        type: string
                      minItems: 0
                      type: array
                    user:
                      minLength: 1
                      type: string
                  required:
                  - actions
                  - user
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - user
                x-kubernetes-list-type: map
              anonymousRead:
                type: boolean
              clusterRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              name:
                pattern: ^[a-z0-9][a-z0-9.-]{1,61}[a-z0-9]$
                type: string
              objectLock:
                default: false
                type: boolean
                x-kubernetes-validations:
                - message: objectLock cannot be disabled once enabled
                  rule: '!oldSelf || self'
              owner:
                type: string
              placement:
                properties:
                  dataCenter:
                    type: string
                  dataNode:
                    type: string
                  diskType:
                    type: string
                  fsync:
                    type: boolean
                  rack:
                    type: string
                  readOnly:
                    type: boolean
                  replication:
                    pattern: ^[0-9]{3}$
                    type: string
                  ttl:
                    pattern: ^[1-9][0-9]*[mhdwMy]$
                    type: string
                  volumeGrowthCount:
                    format: int32
                    minimum: 0
                    type: integer
                  worm:
                    type: boolean
                type: object
              quota:
                properties:
                  enforce:
                    default: true
                    type: boolean
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - size
                type: object
                x-kubernetes-validations:
                - message: quota.size must be non-negative
                  rule: '!string(self.size).startsWith(''-'')'
              reclaimPolicy:
                default: Retain
                enum:
                - Retain
                - Delete
                type: string
              versioning:
                default: "Off"
                enum:
                - "Off"
                - Enabled
                - Suspended
                type: string
                x-kubernetes-validations:
                - message: cannot disable versioning once it has been Enabled or Suspended;
                    use Suspended instead
                  rule: self != 'Off' || oldSelf == 'Off'
            required:
            - clusterRef
            type: object
            x-kubernetes-validations:
            - message: 'objectLock requires versioning: Enabled'
              rule: '!self.objectLock || self.versioning == ''Enabled'''
          status:
            properties:
              bucketName:
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              objectLockEnabled:
                type: boolean
              observedGeneration:
                format: int64
                type: integer
              ownerIdentity:
                type: string
              phase:
                enum:
                - Pending
                - Ready
                - Failed
                - Terminating
                type: string
              quota:
                properties:
                  enforced:
                    type: boolean
                  sizeBytes:
                    format: int64
                    type: integer
                type: object
              usage:
                properties:
                  lastUpdated:
                    format: date-time
                    type: string
                  objectCount:
                    format: int64
                    type: integer
                  sizeBytes:
                    format: int64
                    type: integer
                type: object
              versioning:
                enum:
                - "Off"
                - Enabled
                - Suspended
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
