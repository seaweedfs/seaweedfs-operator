resources:
- role.yaml
- role_binding.yaml
- leader_election_role.yaml
- leader_election_role_binding.yaml
# The following RBAC protects the /metrics endpoint, which the manager serves
# directly with authn/authz (controller-runtime), so only clients granted the
# metrics-reader role can scrape it. Comment them out to disable the protection.
# More info: https://book.kubebuilder.io/reference/metrics.html
- metrics_auth_role.yaml
- metrics_auth_role_binding.yaml
- metrics_reader_role.yaml
