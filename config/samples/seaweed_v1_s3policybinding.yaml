apiVersion: seaweed.seaweedfs.com/v1
kind: S3PolicyBinding
metadata:
  name: alice-uploads-binding
  namespace: default
spec:
  seaweedRef:
    name: seaweed-sample
  # The S3Policy to attach (by resource name, same namespace). A name with
  # no matching S3Policy is used as the IAM policy name directly.
  policyRef:
    name: rw-uploads
  # The identities the policy is attached to. The controller reconciles to
  # exactly this set — identities removed here have the policy detached.
  # Add more subjects (each must reference an existing S3Identity), e.g.:
  #   - kind: S3Identity
  #     name: bob
  subjects:
    - kind: S3Identity
      name: alice
  # reclaimPolicy: Retain  # default Delete: removing this CR detaches the
  #                        # policy from all listed subjects
